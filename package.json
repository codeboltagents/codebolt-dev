{
  "name": "hwrld",
  "version": "1.0.0",
  "description": "Can log \"hello world\" and \"goodbye world\" to the console!",
  "main": "./dist/bundle.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "dependencies": {
    "@codebolt/codeboltjs": "^1.1.69"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "node build.js && cp codeboltagent.yaml dist/"
  }
}
