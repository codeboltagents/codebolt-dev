{
  "name": "codeboltdev",
  "version": "1.0.0",
  "description": "Codebolt Dev Application",
  "main": "./dist/bundle.js",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "dependencies": {
    "@codebolt/codeboltjs": "^1.1.69"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "esbuild": "^0.24.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "esbuild ./src/index.ts --bundle --outfile=./dist/index.js --platform=node --format=cjs --sourcemap --target=es2020 && cp codeboltagent.yaml dist/"
  }
}
